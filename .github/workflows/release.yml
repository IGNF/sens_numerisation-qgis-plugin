name: Release QGIS Plugin

on:
  push:
    tags:
      - '*'

jobs:
  build-release:
    runs-on: ubuntu-latest

    permissions:
      contents: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install zip
        run: sudo apt-get install zip -y

      - name: Build plugin zip
        run: |
          mkdir -p IGN_sens_numerisation
          rsync -av --progress . IGN_sens_numerisation/ \
            --exclude .git \
            --exclude .github \
            --exclude IGN_sens_numerisation
          zip -r IGN_sens_numerisation.zip IGN_sens_numerisation

      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          files: "IGN_sens_numerisation.zip"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}